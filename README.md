# Chromatomato
Test change via Codex.
Another test change via Codex.
Random test change.

## Requirements
- Python 3.10+
- matplotlib

## Run
- Windows: `run.bat` or `run.ps1`
- Or from the `data` folder: `python ..\plot_chromatogram_click_select_place_keepalive.py`
